Now I'll resolve the two conflict regions with targeted edits.Both conflict regions are resolved. The merge combines G2's WireMetrics/keepalive `_peer_protocol_version` tracking with G3's history-index differential `_peer_supports_reduction` tracking — both are independent features that need to coexist, and the imports now include `List` (G2), `Tuple` (G3), `history` (G3), and `WireMetrics` (G2).