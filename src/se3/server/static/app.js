Resolved the conflict in `src/se3/server/static/app.js:14`. The only conflict was in the `state` object initializer — HEAD (G4) added `drawerConversationRecords` and the G5 branch added `connStale`. Both keys are needed: `drawerConversationRecords` backs the running-flow drawer conversation (used by `openDrawer`/`applyHistoryData`) and `connStale` backs the WS stale-banner logic (used by `setStale`/`connect`). Kept both. No conflict markers remain.

You can now `git add` the file to mark it resolved.