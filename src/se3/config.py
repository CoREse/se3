Resolved the conflict in src/se3/config.py:MergeConfig — kept HEAD's `strategy: str = "fast"` (matches the task spec's new default) and G5's flipped `delete_merged_default: bool = True` with its accompanying comment.