I resolved the conflict in `analyze.py`. Both branches added equivalent `se3 code-index search` guidance to the ANALYZE_PROMPT; I kept the G2 phrasing (cleaner "grep 'pattern' se3/code-index.md" replacement framing) and removed all conflict markers. The rest of the file was identical on both sides and is preserved unchanged.