Resolved the conflict in `human_call.py` by combining both branches: kept G4's directory `fsync` after `os.replace` plus `_read_original_for_orphan` / `_is_spec_path` helpers, and merged the temp-file cleanup using an `except: ... raise` block (matching K9's intent — only unlink on actual failure).