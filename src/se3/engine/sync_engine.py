Both conflicts in `sync_engine.py` are resolved. The `SpecAnalysis` dataclass now keeps both fields from the two groups — `touched_files` (G5/HEAD) and `code_fully_absent` (G6) — in `to_dict` and `from_dict`.