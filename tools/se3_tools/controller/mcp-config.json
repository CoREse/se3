{
  "mcpServers": {
    "se3-controller": {
      "command": "python3",
      "args": ["-m", "se3_tools.controller.mcp_server"],
      "env": {
        "SE3_PROJECT_ROOT": "${PROJECT_ROOT}",
        "SE3_SESSION_ID": "${SESSION_ID}",
        "SE3_AGENT_ROLE": "${AGENT_ROLE}"
      }
    }
  }
}
