Conflict resolved. I picked the impl/G6 branch's assertions throughout — they check for the new `## Task Plan`/`## Task Summary`/`## Dependencies` markdown headings (consistent with the Panel→Group/heading refactor described in the task and G6's group summary), and the empty-tasks test now uses console capture to check for both the heading and "No tasks" body.