I can see the conflict in `tests/engine/test_issue_discovery.py` at lines 237-255. Let me understand both sides before resolving.Now I understand the conflict. Let me check what G1 added regarding scope in the Issue model.Good. The conflict is clear:

- **HEAD (G1)**: Test uses `verify_spec` step in `collect_issues_from_output`, but verify_spec has been removed from `ISSUE_DISCOVERY_STEPS` whitelist. This test would fail because `collect_issues_from_output` returns `[]` for non-whitelist steps. It also has a partial `test_summarize_source_tag` using old `"info"` priority.
- **G2**: Test correctly uses `summarize` step and `"warning"` as the invalid priority hint.

The correct resolution is to take G2's version — `summarize` is in the whitelist, `"warning"` is a valid invalid-hint test value, and tag/scope testing is already covered by `test_adds_correct_tags` (line 276).Now let me verify the file looks correct and run the tests.All 33 tests pass. The conflict is resolved.

**Resolution**: Took G2's version. HEAD (G1) had tests using `verify_spec` in `collect_issues_from_output`, but since verify_spec was removed from the B-class whitelist (`ISSUE_DISCOVERY_STEPS`), those tests would have failed. G2 correctly uses `summarize` (which is in the whitelist). The scope/tag assertions from G1 are already covered by existing tests (`test_adds_correct_tags`, `test_verify_spec_no_longer_in_whitelist`).Conflict resolved and staged. The merge can proceed.