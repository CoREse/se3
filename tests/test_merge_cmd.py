Resolved the conflict in `tests/test_merge_cmd.py:262-264`. Combined HEAD's `strategy == "fast"` (G2 strategy rename) with G5's `delete_merged_default is True` (default flip) and kept G5's explanatory comment.