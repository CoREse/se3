Now I'll write the resolved file:Resolved. Both branches' additions are kept: the resume-flow guardrails (section 6a) and the G7 issues-view guardrails (section 6b). I de-duplicated the `_read_style_css` helper (G5's copy was redundant with the one already defined in section 3) and kept the unique `_read_index_html` helper, so all conflict markers are gone and there are no duplicate function definitions.