I've resolved the merge conflict by combining both sides: the docstring now describes the shared home for both test surfaces, the imports merge both modules, and both the charter subsystem tests (G3) and the `CodeIndexConfig` knob tests (G8) are kept. This matches the HEAD docstring's stated intent that charter-specific tests would join once the charter module landed.