Resolved. I merged both sides: G8's `CodeIndexConfig` config-knob tests and G3's charter-subsystem tests now live together in `tests/test_charter.py`, with combined imports (`sys`/`Path`/both modules), a unified docstring explaining the shared home, and a single `if __name__ == "__main__"` runner at the end.