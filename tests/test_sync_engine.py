Let me read the full conflict file and the other modified files to understand the complete picture.Let me read the full test file with the conflict markers to get the exact content.Now I have the full picture. The conflict is at the end of the file — I need to include both G1 and G2 test sections. Let me resolve it.Now let me verify no conflict markers remain and run the tests.Conflict resolved. All 96 tests pass. Both G1 tests (strip_markdown_fences, length guard, fence stripping) and G2 tests (normalize_for_matching, extract_spec_name, normalized idempotency, lifecycle three-layer matching) are included. I removed the `G2:` prefixes from section headers to keep naming consistent.