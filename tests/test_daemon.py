The merge conflict is resolved. Both test classes — `TestDaemonLogging` (from G4) and `TestDaemonStartWarnings` (from G1) — are kept, as they cover independent features and don't conflict semantically.